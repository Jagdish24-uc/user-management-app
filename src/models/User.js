// src/models/User.js
export default class User {
    constructor(id, username, role, position, password) {
      this.id = id;
      this.username = username;
      this.role = role;
      this.position = position;
      this.password = password;
    }
  }
  